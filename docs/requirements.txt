# Documentation build only. The API reference is generated by static analysis,
# so the laiser package and its model dependencies are not needed to build docs.
# MkDocs 2.0 removes the plugin system that mkdocstrings and Material depend on.
mkdocs>=1.6,<2
mkdocs-material>=9.5
mkdocstrings[python]>=0.26
